{
  "name": "odyssey-lift-off-part2",
  "private": true,
  "description": "Root tooling for formatting the Lift-off starter and final apps",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
